// Moved to @beyou/state/dashboard/helpers so web + mobile share one source.
// Re-exported here so existing imports (./getGreetingKey) keep working.
export { getGreetingKey, type GreetingKey } from "@beyou/state/dashboard/helpers";
